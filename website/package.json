{
  "name": "polishly-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && node scripts/dev.mjs"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
